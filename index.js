import React from "react";
import ReactDOM from "react-dom";
import TodoList from "./src/components/TodoList"; // Путь к TodoList компоненту

ReactDOM.render(<TodoList />, document.getElementById("root")); // Рендерим TodoList в корневой элемент
